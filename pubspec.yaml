name: dart_utils
description: Dart utils
version: 2.2.1
homepage:

environment:
  sdk: ^3.10.1
  
dependencies:
  protobuf: ^6.0.0
  http: ^1.0.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  mock_http:
    git:
      url: https://github.com/dumabg/mock_http.git
